# To create three pages to save for a combo
# Step 1 create a sub menu for roll the dice asking if they are rolling a saved combination or not if no proceed to dice roll
# if yes ask for input for dice roll eg sneak attack rolls 3d6 when called
# Need a page for them to create and save combos (can be done on functions page)
# Import JSON and create a page to view saved rolls


# while len(name)>0:
#       print("name accepted")
#   try:
#         name=input
#         while len(input)> 0:
#             name=len(input>0)("Enter the name of the dice combo:\n")
#         if len(input)==0:
#             raise NoInput("Input can't be nothing please enter a name")
#     except NoInput as e:
#         print("Input can't be nothing please enter a name")
